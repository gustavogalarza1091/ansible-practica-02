---

- name: installing kubernates master
  hosts: master
  vars_files:
    - "group_vars/common.yaml"
    - "group_vars/common-podman.yaml"
  gather_facts: false
  roles:
    - common
    - common-podman
    - master
  tasks:
    - name: kubeadm init --pod-network-cidr 192.169.0.0/16 --ignore-preflight-errors=all
      shell: kubeadm init --pod-network-cidr 192.169.0.0/16  --ignore-preflight-errors=all| grep kubeadm | awk '{print $1}'
      register: kubeadm_init
      
    - debug: 
        var: kubeadm_init.output